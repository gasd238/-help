<!--메인 페이지-->
<!DOCTYPE html>
<html>
  <head>
    <title>Help</title>
    <link rel='stylesheet' href='/stylesheets/index.css' />
  </head>
  
  <body>
    <div class="top-bar">
      <% if(islogin == 'login'){ %>
      <a href='./users/logout' class="deco-none">로그아웃</a>
      <% } else { %>
      <a href='./login' class="deco-none">로그인</a>
      <% } %>
      | <a href='./mypage' class="deco-none">마이페이지</a>
      | <a href="./exchange" class="deco-none">교환소입니다!</a>
    </div>
    <div class="title">
        <a href="/"><h1>Help</h1></a>
    </div>
    <p>Welcome to Help</p>
    <!-- <a class="dump" href="/test">(임시)test1으로 가기</a><br>
    <a class="dump" href="/test2">(임시)test2로 가기</a> -->
    <% if(post != null) { %>
      <% post.reverse(); %>
      <% for(i = 0; i < post.length; i++) { %>
        <hr>
        <a href="/readpost/<%= post[i].key %>"><p>제목: <%= post[i].title %></p></a>
        <hr>
      <% } %>
    <% } %>

    <% if(islogin == 'login'){ %>
      <a href="./writepost">글쓰기</a>
    <% } %>
    </div>
    <br>
  </body>
</html>