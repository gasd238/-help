<!DOCTYPE html>
<html lang="ko">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>!Help</title>
    <link href="/bootstrap/css/bootstrap.min.css" rel="stylesheet" id="bootstrap-css">
    <link rel='stylesheet' href='/stylesheets/Mypage.css' />
    <link rel='stylesheet' href='/stylesheets/index.css' />

</head>
<body>
    <div class="title">
        <a href="/"><h1>Help</h1></a>
    </div> <!--이거 추가했어요-->
    <div class="container h-100">
        <div class="row justify-content-center align-items-center h-100">
            <div class="col-12 col-sm-8 col-md-10 col-lg-20">
                <div class="profile-card py-3 card shadow text-center">
                    <div class="card-body py-4">
                        <a href="./users/editprofile"><img class="profile-picture rounded-circle"
                                src="Images/basic_images.jpg" /></a>
                        <h2 class="text-warning h4 font-weight-bold mt-3 mb-1">
                            <%= PInfo.name %>
                        </h2>
                        <p class="text-muted text-uppercase font-weight-light">
                            포인트 : <%= PInfo.point %> / 봉사시간 : <%= PInfo.vtime %> / <% if(PInfo.is_volunteer) { %>
                                봉사자입니다.
                                <% }else{ %>
                                봉사자가 아닙니다.
                                <% } %>
                        </p>
                        <p class="px-3 mt-4">
                            <form method="POST" action="./mypage">
                                <h1>수정사항</h1>
                                <input type="hidden" name="is_vol" value=<%= PInfo.is_volunteer %>>
                                Are you volunteer?
                                <p><input type="submit" class="submit" value=<% if(PInfo.is_volunteer) {%>
                                    "No"
                                    <% }else{ %>
                                    "Yes"
                                    <% } %>
                                    ></p>
                            </form>
                            <br>
                            <form method="POST" action="./users/upvol">
                                <h1>봉사시간 업로드</h1>
                                업로드를 하면 가지고 있는 시간이 0이 됩니다
                                <p><input type="submit" value="실행"></p>
                            </form>
                        </p>
                    </div>
                </div>
            </div>
            <div class="article">
                <b>작성한 글 목록</b> <!--이것도 추가했어요-->
                <br>
                    <% if(post != null) { %>
                        <% post.reverse(); %>
                        <% for(i = 0; i < post.length; i++) { %>
                            <div class="article2"><h3><%= post[i].title %></h3><%= post[i].post %></div>
                        <% } %>
                    <% } %>
                
                <br>
                
            </div>
        </div>
    </div>

    </div>
</body>

</html>